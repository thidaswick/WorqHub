/**
 * Deployment smoke check. From the repo root run: node test.js
 * Exits with code 0 when the environment can execute Node successfully.
 */
console.log("[WorqHub] deployment test OK", new Date().toISOString());
process.exit(0);
